spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://ballast.proxy.rlwy.net:35469/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ClpcokuCeSYqKQFHVCtzsOmSYmuESMRT}
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Minimal CORS for testing (allow same-origin by default)
# app.apiKey is used for /api/driver/* endpoints
app.apiKey=${DRIVER_API_KEY:AlphaBus-2025!}

spring.jpa.hibernate.ddl-auto=update
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Razorpay Payment Gateway
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_ScY5ZlEYPNT8vw}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:Zac0glNDqqNrt0X1MPTv4p0K}

# File Upload sizes
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
