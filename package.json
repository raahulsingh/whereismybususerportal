{
  "name": "where-is-my-bus-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix whereismybusv2",
    "start": "npm run dev --prefix whereismybusv2",
    "build": "npm run build --prefix whereismybusv2",
    "lint": "npm run lint --prefix whereismybusv2",
    "preview": "npm run preview --prefix whereismybusv2"
  }
}
